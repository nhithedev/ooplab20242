The Aims class must be updated to handle any exceptions generated when the play() methods are called. What happens when you don’t update for them to catch? 
(in Exception part)

Answer:
If you do not catch exceptions generated by play() in the Aims class, the program will terminate abnormally (crash) and show a stack trace, and any subsequent code will not run.